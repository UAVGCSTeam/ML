opencv-python
numpy==2.0
argparse
ultralytics
torch 
torchvision
# torch-tensorrt --index-url https://pypi.ngc.nvidia.com # only needed for the torch to tensorrt conversion, not if running the model as stand
